[ 화물(생산품/자원) 아이콘을 여기에 넣으세요 ]

★ 이 이미지들은 "맵 위 화물"의 실제 그래픽입니다.
   ★ 애니메이션 없음 — 한 파일 = 정적 이미지 1장(프레임 분할 안 함).
   ★ 권장 크기: 정사각형 PNG(예: 48x48 또는 64x64), 투명 배경.
     게임이 화물 위치에 약 34px(중심 정렬)로 그려줍니다.

파일명(그대로 / js/config.js 의 G.PRODUCTS.img):
  meat.png     ← 실장육      minced.png ← 분쇄육
  food.png     ← 실장푸드(사료)   unchi.png  ← 운치
  skewer.png   ← 꼬치훈제    can.png    ← 통조림
  vacuum.png   ← 진공포장    jeotgal.png ← 실장젓갈
  muchim.png   ← 실장무침

* 파일이 없으면 색 사각형 + 글자 플레이스홀더로 표시됩니다.
* 주의: 장치 '꼬챙이'는 devices/skewer_dev.png (여기 products/skewer.png 와 다름).
